{% from "../../macros/attributes.njk" import govukAttributes %}
{# TODO #}
{#{% from "../hint/macro.njk" import govukHint %}#}
{#{% from "../label/macro.njk" import govukLabel %}#}


{% if not params.layout or params.layout === "vertical" %}
    {% set vertical = true %}
{% endif %}

{% if params.layout === "text" %}
    {% set text = true %}
{% endif %}

{% if text %}
    {% set signpostClass = "govuk-signpost govuk-signpost__card--text"%}
{% else %}
    {% set signpostClass = "govuk-signpost govuk-signpost__card {%- if vertical %} govuk-signpost__card--vertical {% endif %}" %}
{% endif %}

{% if params.image %}
    {% set image = params.image%}
{% endif %}

{% if params.asLink %}
<a class='{{ signpostClass }} govuk-signpost__link' href="#">
{% else %}    
<div class='{{ signpostClass }}' href="#">
{% endif %}  
    {% if params.icon %}
        <div class='govuk-signpost__icon {%- if vertical %} govuk-signpost__icon--vertical {% endif %}'>
            {{ params.icon | safe | trim | indent() }}
        </div>
    {% endif %}
    <div class='govuk-signpost__container'>
        <div>
            {% if image %}
            <img src='{{ image }}' class='govuk-signpost__image' alt='placeholder'/>
            {% endif %}
            {% if text %} <a href="#"> {% endif %}
            <h3 class='govuk-signpost__title'>Dlhý nadpis</h3>
            {% if text %} <a/> {% endif %}
            {% if text and params.subtitle%}
            <p class='govuk-signpost__subtitle'>{{ params.subtitle }}</p>
            {% endif %}
            <p class='govuk-signpost__description'>Tento text slúži iba ako ukážka naplneného obsahu v danom komponente. Nepoužívať pri implementácii a dizajne.</p>    
            {% if params.actions %}
            <div class='govuk-signpost__actions'>
                {{ params.actions | safe | trim | indent(4) }}
            </div>
            {% endif %}
            {% if text and params.tag%}
            <p class='govuk-signpost__tag'>{{ params.tag }}</p>
            {% endif %}       
        </div>
        {% if params.withArrow %}
            <div class='govuk-signpost__arrow-icon'>
                <span class="material-icons">east</span>
            </div>
        {% endif %}
    </div>
{% if params.asLink %}
</a>
{% else %}    
</div>
{% endif %}  

{# {% if params.withAction %}
    <div class='govuk-signpost'>
        {% if params.withIcon%}
        <div class='govuk-signpost__icon'>
            <span class="material-icons">home</span>
        </div>
        {% endif %}
        <div class='govuk-signpost__body'>
            <div class='govuk-signpost__header'>
                <div class='govuk-signpost__title-wrapper'>
                    {% if params.withImage%}
                        <img src='../../assets/images/placeholder.png' alt="Placeholder"/>
                    {% endif %}
                    <h3 class='govuk-signpost__title'>Dlhý nadpis</h3>

                    {% if params.withArrow%}
                        <span class="material-icons">arrow-forward</span>
                    {% endif %}
                </div>
            </div>
            <p class='govuk-signpost__body'>Tento text slúži iba ako ukážka naplneného obsahu v danom komponente. Nepoužívať pri implementácii a dizajne.</p>
            <button class="govuk-button govuk-button__basic" aria-label="Akcia">
                Akcia
            </button>
        <div>
    </div>
{% else %}
    <a class='govuk-signpost govuk-signpost--link {%- if params.horizontal %} govuk-signpost--horizontal{% endif %}' href="#">
        {% if params.withIcon%}
        <div class='govuk-signpost__icon'>
            <span class="material-icons">home</span>
        </div>
        {% endif %}
        <div class='govuk-signpost__body'>
            <div class='govuk-signpost__header'>
                <div class='govuk-signpost__title-wrapper'>
                    {% if params.withImage%}
                        <img src='../../assets/images/placeholder.png' alt="Placeholder"/>
                    {% endif %}
                    <h3 class='govuk-signpost__title'>Dlhý nadpis</h3>

                    {% if params.withArrow%}
                        <span class="material-icons">arrow-forward</span>
                    {% endif %}
                </div>
            </div>
            <p class='govuk-signpost__body'>Tento text slúži iba ako ukážka naplneného obsahu v danom komponente. Nepoužívať pri implementácii a dizajne.</p>
        <div>
    </a>
{% endif %} #}
